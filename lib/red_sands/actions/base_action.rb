# typed: strict
# frozen_string_literal: true

module RedSands
  module Actions
    # this may be useful later to distinguish between events that are player-originated or something
    # as of now it serves no purpose
    class BaseAction < Effect
    end
  end
end
